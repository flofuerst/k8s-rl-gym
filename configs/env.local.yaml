namespace: default
deployments:
  - nginx-demo
  - nginx-worker
min_replicas: 1
max_replicas: 3
metrics:
  - desired_replicas
  - ready_replicas
  - available_replicas
  - unavailable_replicas
  - pod_count
step_wait_seconds: 2.0
max_steps: 10
reward: readiness
